import { useEffect, useState } from "preact/hooks";
import Similarity from "./similarity";
const App = () => {
  
  return (
    <div id="app">
      <Similarity />
    </div>
  );
};

export default App;
